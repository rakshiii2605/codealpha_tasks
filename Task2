## 📌 Task 2: Event Registration System (Backend)

This project is a backend **Event Registration System** built as part of my **Backend Developer Internship**.  
It allows users to register, authenticate, view events, and manage their event registrations through RESTful APIs.

---

## 🚀 Tech Stack
- **Node.js**
- **Express.js**
- **MongoDB (Mongoose)**
- **JWT Authentication**
- **Postman (API Testing)**

---

## ⚙️ Features
- User authentication (Register & Login)
- Create and view events
- Register users for events
- View user-specific registrations
- Cancel event registrations
- Secure routes using JWT

---

## 📁 API Endpoints

### 🔐 Authentication
```http
POST /api/auth/register
POST /api/auth/login

### 📅 Events
POST /api/events
GET /api/events

### 📝 Registrations
POST /api/register
GET /api/register/:userId
DELETE /api/register/:id

### 🗂️ Environment Variables

Create a .env file in the root directory:

MONGO_URI=your_mongodb_connection_string
JWT_SECRET=your_secret_key

### ▶️ How to Run Locally
npm install
npm run dev

---

## 🧪 API Testing

All APIs were tested using Postman to verify authentication, event creation, registration, and deletion.
